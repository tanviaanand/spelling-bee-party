// Copy this file to src/firebase-config.js:
//   cp src/firebase-config.example.js src/firebase-config.js
//
// With the placeholders left as-is, the app runs in LOCAL TEST MODE — everything
// works in one browser (great for trying it out). To go live for real phones,
// create a free Firebase project (see README) and paste your web-app config here.
// This config is not a secret — access control is the database rules, not this file.
export const firebaseConfig = {
  apiKey: "PASTE_ME",
  authDomain: "PASTE_ME",
  databaseURL: "PASTE_ME",
  projectId: "PASTE_ME",
  storageBucket: "PASTE_ME",
  messagingSenderId: "PASTE_ME",
  appId: "PASTE_ME",
};
